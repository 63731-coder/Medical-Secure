# Variables d'environnement pour le frontend Vue.js
# Ces variables sont exposées au client (préfixées par VITE_)

# URL de l'API backend (via nginx avec HTTPS)
VITE_API_URL=https://localhost/api

# URL de Keycloak (via nginx avec HTTPS)
VITE_KEYCLOAK_URL=https://localhost/auth
